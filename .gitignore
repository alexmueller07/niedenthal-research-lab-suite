# Dependencies / build output
node_modules/
dist/
src-tauri/target/
# Regenerated by tauri-build on every compile.
src-tauri/gen/schemas/

# FFmpeg sidecars are downloaded by scripts/fetch-ffmpeg.mjs, never committed.
# They are ~80 MB each and platform-specific; the pinned version + checksum in
# scripts/ffmpeg-manifest.json is the thing under version control.
src-tauri/binaries/

# Recorded media and manifests are study data under IRB 2020-1657 and belong on
# the UW Research Drive, never in a git repo.
*.mp4
*.mkv
*.mov
*.wav
*.webm
recordings/
scratch/

# Local machine settings (paths, device fingerprints, shared secret).
lab-recorder.local.json
.env

# OS / editor
.DS_Store
Thumbs.db
